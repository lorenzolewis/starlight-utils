---
import { compiledContent } from "../../../packages/starlight-utils/CHANGELOG.md";

const content = (await compiledContent()).replace(/(<h1.*?>.*?<\/h1>)/, "");
---

<Fragment set:html={content} />
